specVersion: "v1beta"
serviceId: "daily-micro-learning"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "daily-micro-learning-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
